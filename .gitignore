# Env — NEVER commit real credentials
.env

# Build output
dist/
node_modules/

# Evidence files — private data
evidence/
backups/

# Prisma client (generated)
node_modules/.prisma/

# SQLite database files (local dev)
*.db
*.db-journal
# 20260714 RG - Ancorato alla radice: senza lo slash iniziale la regola prendeva
# anche src/data/, cioe' il catalogo broker, che DEVE stare nel repo (seed.ts lo
# importa e senza di lui `npm run build` non compila).
/data/

# OS
.DS_Store
Thumbs.db
